# 📚 توثيق مشروع ادرس — مشروع التخرج

> منصة تعليمية يمنية تتيح للطلاب قراءة الكتب المدرسية وتتبع تقدمهم، مع لوحة إدارة للمعلمين وأولياء الأمور.

---

## 🗂️ هيكل المشروع العام

```
project1/
├── adres_backend_v1.0.1/          ← الخادم (Django REST Framework)
│   └── yemeni_school_backend/
│       ├── config/                ← إعدادات المشروع + URLs الرئيسية
│       ├── students/              ← تطبيق الطلاب
│       ├── teachers/              ← تطبيق المعلمين
│       ├── parents/               ← تطبيق أولياء الأمور
│       ├── curriculum/            ← المناهج (مواد، كتب، صفحات، ملخصات)
│       ├── progress/              ← تتبع تقدم الطلاب
│       └── db.sqlite3             ← قاعدة البيانات
│
├── adres_flutter_v1.0.1/          ← تطبيق الطالب وولي الأمر والمعلم (Flutter)
│   └── lib/
│       ├── main.dart
│       ├── core/                  ← خدمات مشتركة، ثيم، إعدادات
│       └── features/              ← شاشات كل نوع مستخدم
│
└── adres_admin_flutter/           ← تطبيق الإدارة (Flutter)
    └── lib/
        ├── main.dart
        ├── core/
        └── features/
```

---

## 🔧 الباك إند — Django REST Framework

### التقنيات المستخدمة

| التقنية | الاستخدام |
|---|---|
| Python 3 + Django | إطار العمل الرئيسي |
| Django REST Framework | بناء الـ API |
| django-cors-headers | السماح للتطبيق بالاتصال بالسيرفر |
| SQLite | قاعدة البيانات |
| Groq API (llama-3.3-70b) | الذكاء الاصطناعي لشرح النصوص |

### الإعدادات الرئيسية (`config/settings.py`)

- اللغة: `ar`، المنطقة الزمنية: `Asia/Aden`
- `CORS_ALLOW_ALL_ORIGINS = True` — يسمح لأي جهاز بالاتصال
- `REST_FRAMEWORK`: لا يوجد مصادقة (AllowAny) — مناسب للبيئة المدرسية المغلقة
- `DEBUG = True` افتراضياً، يُغيَّر عبر متغير البيئة
- مفتاح Groq يُحمَّل من متغير البيئة `GROQ_API_KEY`

---

## 📦 تطبيقات الباك إند (Apps)

### 1. `students` — الطلاب

**النموذج (`Student`):**

| الحقل | النوع | الوصف |
|---|---|---|
| `academic_id` | CharField (unique) | الرقم الأكاديمي، يُولَّد تلقائياً |
| `full_name` | CharField | الاسم الكامل |
| `grade_level` | CharField | الصف الدراسي |
| `class_id` | CharField | معرف الشعبة (مثال: class_9A) |
| `class_name` | CharField | اسم الشعبة (مثال: التاسع - أ) |
| `password` | CharField (nullable) | كلمة المرور (نص عادي) |
| `created_at` | DateTimeField | تاريخ الإنشاء |

**الـ API:**

| الطريقة | المسار | الوصف |
|---|---|---|
| GET | `/api/students/` | جلب كل الطلاب |
| GET | `/api/students/?academic_id=xxx` | البحث برقم أكاديمي |
| POST | `/api/students/` | إضافة طالب جديد |
| GET | `/api/students/<id>/` | جلب طالب واحد |
| PUT | `/api/students/<id>/` | تعديل بيانات طالب |
| DELETE | `/api/students/<id>/` | حذف طالب |
| PATCH | `/api/students/<id>/update_class/` | تحديث شعبة الطالب |
| POST | `/api/students/<id>/set_password/` | تعيين/تغيير كلمة المرور |

**منطق توليد الرقم الأكاديمي:**
يأخذ أعلى رقم موجود ويضيف 1، القيمة الافتراضية الأولى `78246`.

---

### 2. `teachers` — المعلمون

**النماذج:**

- **`Teacher`**: رقم وظيفي، كلمة مرور (مشفرة بـ `make_password`)، اسم، مادة
- **`TeacherStudent`**: علاقة many-to-many بين المعلم والطالب مع اسم الشعبة
- **`TeacherClass`**: شعب المعلم (class_id، class_name، عدد الطلاب)

**الفرق عن الطالب:** كلمة مرور المعلم مشفرة بـ Django hashing، بينما كلمة مرور الطالب نص عادي.

**الـ API الرئيسية:**

| المسار | الوصف |
|---|---|
| `POST /api/teachers/login/` | تسجيل دخول المعلم |
| `GET /api/teachers/` | جلب المعلمين |
| `POST /api/teachers/<id>/add_student/` | إضافة طالب للمعلم |
| `DELETE /api/teachers/<id>/remove_student/<academic_id>/` | إزالة طالب |
| `POST /api/teachers/<id>/add_class/` | إضافة شعبة |
| `DELETE /api/teachers/<id>/remove_class/<class_id>/` | إزالة شعبة |

---

### 3. `parents` — أولياء الأمور

**النموذج (`Parent`):**
- `parent_id`: رقم ولي الأمر (unique)
- `full_name`: الاسم
- `children`: ManyToMany → Student

**الـ API:**

| المسار | الوصف |
|---|---|
| `GET /api/parents/?parent_id=xxx` | البحث بالرقم |
| `POST /api/parents/<id>/add_child/` | إضافة ابن |
| `DELETE /api/parents/<id>/remove_child/<academic_id>/` | إزالة ابن |

---

### 4. `curriculum` — المناهج الدراسية

**النماذج (من الأكبر للأصغر):**

```
Subject (مادة)
  └── Book (كتاب - فصل أول/ثاني)
        └── BookPage (صفحة كتاب - HTML)
              └── PageSummary (ملخص نوع 1/2/3)
                    └── PageSummaryPage (صفحة الملخص - HTML)
```

**تفاصيل النماذج:**

- **Subject**: اسم، صف دراسي، مرحلة (ابتدائي/إعدادي/ثانوي)، ترتيب عرض
- **Book**: مرتبط بمادة، عنوان، فصل (أول/ثاني)، عدد الصفحات
- **BookPage**: محتوى HTML لكل صفحة، unique على (book, page_number)
- **PageSummary**: 3 أنواع ملخصات لكل صفحة
- **PageSummaryPage**: صفحات الملخص بترتيب، كل ملخص قد يكون متعدد الصفحات

**الـ API:**

| المسار | الوصف |
|---|---|
| `GET /api/subjects/` | جلب المواد |
| `GET /api/books/?subject=1&term=first` | جلب الكتب |
| `GET /api/page/?book=1&page=5` | جلب صفحة كتاب (JSON) |
| `GET /page/<number>/?book=1` | عرض صفحة كتاب (HTML مباشر) |
| `GET /api/summaries/?page=<id>` | جلب ملخصات صفحة |
| `GET /summary/?book=1&page=5&type=1&summary_page=1` | عرض صفحة ملخص (HTML) |

**ملاحظة:** صفحات الكتاب والملخصات تُعرض كـ HTML مباشر داخل WebView في التطبيق.

---

### 5. `progress` — تتبع التقدم

**النموذج (`Progress`):**

| الحقل | الوصف |
|---|---|
| `academic_id` | الرقم الأكاديمي (مفهرس) |
| `book` | FK → Book |
| `last_page` | آخر صفحة وصل إليها |
| `pages_read` | عدد الصفحات المفتوحة |
| `total_time_minutes` | إجمالي وقت القراءة |
| `interaction_score` | نقاط التفاعل |
| `progress_percent` | نسبة الإنجاز (0.0 → 1.0) |

**منطق الحفظ:** عند التحديث لا يُستبدل، بل يُؤخذ الأعلى (`max`) للصفحات والنسبة، ويُجمع الوقت والنقاط.

**الـ API:**

| المسار | الوصف |
|---|---|
| `GET /api/progress/?academic_id=xxx&book=1` | جلب تقدم طالب في كتاب |
| `POST /api/progress/` | حفظ/تحديث تقدم |
| `GET /api/progress/student/<academic_id>/` | كل تقدم طالب |
| `GET /api/classes/<class_id>/students/` | طلاب الشعبة مع تقدمهم |

---

### 6. `AI` — الذكاء الاصطناعي

**المسار:** `POST /api/ai/explain/`

**الطلب:**
```json
{
  "text": "النص المراد شرحه",
  "subject": "رياضيات",
  "grade": "التاسع",
  "student_name": "أحمد"
}
```

**الاستجابة:**
```json
{ "result": "شرح النص..." }
```

- يستخدم نموذج `llama-3.3-70b-versatile` عبر Groq API
- المفتاح محفوظ في السيرفر فقط (لا يُرسل للتطبيق)
- يُخصص الشرح بالمادة والصف واسم الطالب

---

## 📱 تطبيق الطالب — Flutter (`adres_flutter_v1.0.1`)

### التقنيات

| Package | الاستخدام |
|---|---|
| `http` | الاتصال بالـ API |
| `hive` + `hive_flutter` | التخزين المحلي |
| `webview_flutter` | عرض صفحات الكتاب HTML |
| `connectivity_plus` | فحص الاتصال بالإنترنت |

### هيكل الـ lib

```
lib/
├── main.dart                    ← نقطة البداية + إدارة الثيم
├── core/
│   ├── account_type_screen.dart ← شاشة اختيار نوع الحساب
│   ├── server_settings_screen.dart ← إعدادات عنوان السيرفر
│   ├── config/app_config.dart   ← عنوان الـ backend
│   ├── constants/               ← ألوان، خطوط
│   ├── theme/
│   │   ├── app_theme.dart       ← الثيم الفاتح والداكن
│   │   └── theme_notifier.dart  ← إدارة تبديل الثيم
│   └── services/
│       ├── api_service.dart     ← كل طلبات الـ API
│       ├── student_local_service.dart  ← حفظ بيانات الطالب محلياً
│       ├── progress_local_service.dart ← حفظ التقدم محلياً
│       ├── sync_local_service.dart     ← مزامنة التقدم مع السيرفر
│       ├── reading_state_local_service.dart ← حفظ آخر موضع قراءة
│       ├── parent_local_service.dart
│       └── teacher_local_service.dart
└── features/
    ├── splash/splash_screen.dart
    ├── student/
    │   ├── student_login_screen.dart
    │   ├── grade_select_screen.dart
    │   ├── student_home_screen.dart
    │   ├── subject_screen.dart
    │   └── book_screen.dart
    ├── teacher/
    ├── parent/
    └── admin/
```

---

### تدفق دخول الطالب

```
SplashScreen (3 ثوانٍ)
    ↓ هل يوجد بيانات محفوظة؟
   نعم → StudentHomeScreen
   لا  → AccountTypeScreen
              ↓ اختار "طالب"
         StudentLoginScreen
              ↓ أدخل الرقم الأكاديمي
         [API: GET /api/students/?academic_id=xxx]
              ↓
         هل عنده كلمة مرور؟
         لا  → شاشة إنشاء كلمة مرور → [API: POST set_password]
         نعم → شاشة إدخال كلمة المرور
              ↓ صحيحة؟
         نعم → StudentHomeScreen
         لا  → رسالة خطأ (يبقى في نفس الشاشة)
```

---

### `StudentHomeScreen` — الشاشة الرئيسية للطالب

**المحتوى:**
- بطاقة بيانات الطالب (الاسم، الصف، الشعبة، الرقم الأكاديمي)
- شريط التقدم العام (متوسط كل المواد)
- زر "أكمل من حيث توقفت" (يفتح آخر كتاب في آخر صفحة)
- شبكة المواد الدراسية (6 مواد) مع نسبة تقدم كل مادة

**الشريط الجانبي (Drawer):**
- تغيير كلمة المرور (يتحقق من الحالية أولاً)
- تبديل الثيم ليلي/نهاري
- الصفوف الدراسية
- تسجيل الخروج

---

### `GradeSelectScreen` — صفحة الصفوف

- تعرض 12 صفاً: الأول → التاسع + أول/ثاني/ثالث ثانوي
- الصف التاسع فقط متاح، الباقي مغلق مع رسالة "سنضيفه قريباً"
- متاحة من الشريط الجانبي فقط

---

### `BookScreen` — شاشة القراءة

- يعرض صفحات الكتاب كـ HTML داخل WebView
- يتتبع الصفحة الحالية ووقت القراءة
- يحفظ التقدم محلياً أولاً ثم يزامن مع السيرفر
- يدعم عرض الملخصات (3 أنواع) لكل صفحة
- يدعم شرح النص المحدد بالذكاء الاصطناعي

---

### إدارة الثيم

- `ThemeNotifier` (ChangeNotifier) يحفظ الاختيار في Hive
- `main.dart` يستمع للتغييرات ويطبق `ThemeMode.dark` أو `ThemeMode.light`
- يوجد `lightTheme` و `darkTheme` في `AppTheme`

---

### التخزين المحلي (Hive Boxes)

| Box | المحتوى |
|---|---|
| `studentBox` | بيانات الطالب + التقدم المحلي + حالة المزامنة |
| `parentBox` | بيانات ولي الأمر |
| `teacherBox` | بيانات المعلم |
| `appBox` | إعدادات التطبيق (الثيم، عنوان السيرفر) |

---

### المزامنة مع السيرفر

- التقدم يُحفظ محلياً أولاً (offline-first)
- `SyncLocalService` يتتبع السجلات غير المزامنة
- عند فتح التطبيق يحاول المزامنة تلقائياً
- يوجد زر مزامنة يدوي في الـ AppBar

---

## 🛠️ تطبيق الإدارة — Flutter (`adres_admin_flutter`)

### الميزات

- إدارة الطلاب (إضافة، تعديل، حذف، تغيير كلمة المرور)
- إدارة المعلمين وشعبهم وطلابهم
- إدارة أولياء الأمور وأبنائهم
- إدارة المواد الدراسية
- سجل العمليات (log) لكل تعديل
- فلترة الطلاب حسب الصف

### `AdminApi` — الاتصال بالسيرفر

- عنوان السيرفر ثابت في الكود: `http://192.168.43.134:8000`
- يدعم GET, POST, PUT, DELETE
- يُرجع `{'ok': true, 'data': ...}` أو `{'ok': false, 'error': ...}`

---

## ❓ أسئلة وأجوبة متوقعة في المناقشة

**س: ما هو مشروع ادرس؟**
ج: منصة تعليمية يمنية تتيح لطلاب المدارس قراءة الكتب المدرسية رقمياً، تتبع تقدمهم، والحصول على شرح بالذكاء الاصطناعي. تتكون من تطبيق للطلاب/المعلمين/أولياء الأمور وتطبيق إدارة منفصل.

**س: لماذا اخترتم Django للباك إند؟**
ج: لأنه إطار عمل متكامل يوفر ORM قوي، لوحة إدارة جاهزة، وDjango REST Framework لبناء API بسرعة. كما أن Python سهل التعلم والتطوير.

**س: لماذا SQLite وليس PostgreSQL أو MySQL؟**
ج: لأن المشروع في مرحلة التطوير والاختبار، وSQLite كافٍ للبيئة المدرسية المحلية. يمكن الانتقال لـ PostgreSQL في الإنتاج بتغيير سطر واحد في الإعدادات.

**س: كيف يعمل نظام كلمة المرور للطالب؟**
ج: أول مرة يدخل الطالب برقمه الأكاديمي، إذا لم تكن له كلمة مرور يُطلب منه إنشاؤها وتُحفظ في قاعدة البيانات. في المرات التالية يُطلب منه إدخالها. المدير يستطيع تغييرها من تطبيق الإدارة.

**س: هل كلمة مرور الطالب مشفرة؟**
ج: حالياً تُحفظ كنص عادي. كلمة مرور المعلم مشفرة بـ Django hashing. يمكن تطبيق نفس التشفير على الطالب مستقبلاً.

**س: كيف يعمل الذكاء الاصطناعي في التطبيق؟**
ج: الطالب يحدد نصاً في الكتاب، يُرسل للسيرفر عبر `POST /api/ai/explain/`، السيرفر يرسله لـ Groq API (نموذج llama-3.3-70b)، ويُرجع الشرح للتطبيق. المفتاح محفوظ في السيرفر فقط لأمان أكبر.

**س: ما هو نموذج الذكاء الاصطناعي المستخدم؟**
ج: `llama-3.3-70b-versatile` من Meta عبر منصة Groq، وهو نموذج مفتوح المصدر سريع ومجاني.

**س: كيف يعمل تتبع التقدم؟**
ج: التقدم يُحسب بناءً على: عدد الصفحات المقروءة، وقت القراءة، نقاط التفاعل. يُحفظ محلياً أولاً (offline-first) ثم يُزامن مع السيرفر. عند التحديث يُؤخذ الأعلى للنسبة والصفحات، ويُجمع الوقت.

**س: لماذا offline-first؟**
ج: لأن الطلاب قد لا يكون لديهم اتصال دائم بالإنترنت. التطبيق يعمل بدون إنترنت ويزامن عند توفر الاتصال.

**س: ما هي المواد المتاحة حالياً؟**
ج: الرياضيات للصف التاسع فقط (الفصل الأول والثاني). باقي المواد والصفوف ستُضاف تدريجياً.

**س: كيف يتحقق المعلم من تقدم طلابه؟**
ج: عبر `GET /api/classes/<class_id>/students/` الذي يُرجع قائمة الطلاب مع نسبة تقدم كل منهم ووقت القراءة وحالته (نشط/متوسط/يحتاج اهتماماً).

**س: كيف يُضاف محتوى الكتب؟**
ج: عبر scripts Python (`import_summary.py`, `import_summary2.py`) تقرأ ملفات HTML وتستوردها لقاعدة البيانات. أيضاً عبر لوحة Django Admin.

**س: ما الفرق بين تطبيق الطالب وتطبيق الإدارة؟**
ج: تطبيق الطالب للقراءة وتتبع التقدم، يستخدمه الطلاب والمعلمون وأولياء الأمور. تطبيق الإدارة لإدارة البيانات (إضافة/تعديل/حذف الطلاب والمعلمين والمواد)، يستخدمه المسؤول فقط.

**س: كيف يتصل التطبيق بالسيرفر؟**
ج: عبر HTTP REST API. عنوان السيرفر قابل للتغيير من إعدادات التطبيق. في الشبكة المحلية يستخدم IP مثل `192.168.43.134:8000`.

**س: هل يوجد مصادقة (Authentication) في الـ API؟**
ج: لا، الـ API مفتوح (AllowAny). هذا مناسب للبيئة المدرسية المغلقة. يمكن إضافة JWT مستقبلاً.

**س: ما هي الصفوف الدراسية المدعومة في التطبيق؟**
ج: الصفوف من الأول حتى التاسع، ثم أول ثانوي وثاني ثانوي وثالث ثانوي (لا يوجد صف عاشر في النظام اليمني). حالياً التاسع فقط متاح.

**س: كيف يعمل نظام الملخصات؟**
ج: كل صفحة كتاب يمكن أن يكون لها 3 أنواع ملخصات. كل ملخص قد يتكون من عدة صفحات. الطالب يختار نوع الملخص ويتصفح صفحاته داخل WebView.

**س: ما هي تقنية عرض الكتب؟**
ج: صفحات الكتاب مخزنة كـ HTML في قاعدة البيانات وتُعرض داخل `WebView` في Flutter. هذا يتيح تنسيقاً غنياً (جداول، صور، معادلات).

**س: كيف يعمل نظام الشعب والمعلمين؟**
ج: المعلم لديه قائمة شعب (`TeacherClass`) وقائمة طلاب (`TeacherStudent`). يستطيع إضافة/إزالة طلاب وشعب. يرى تقدم طلاب شعبته.

**س: ما هي أبرز التحسينات المستقبلية؟**
ج: تشفير كلمة مرور الطالب، إضافة باقي المواد والصفوف، نظام إشعارات، تقارير تفصيلية للمعلمين، نشر السيرفر على الإنترنت بدلاً من الشبكة المحلية.
